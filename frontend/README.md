# Bookoffe - Projeto Final


Arquivos: frontend (index.html, style.css, app.js) e backend (index.js, package.json).


1. Configure o Supabase: crie as tabelas 'usuarios' e 'cartoes' conforme o plano.
2. No backend, adicione as variáveis de ambiente SUPABASE_URL e SUPABASE_KEY ou substitua os placeholders.
3. Rode `npm install` dentro de /backend e depois `npm start`.
4. Abra /frontend/index.html (ou use Live Server).


